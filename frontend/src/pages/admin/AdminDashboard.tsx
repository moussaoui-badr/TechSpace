export function AdminDashboardPage() {
  return (
    <div>
      <h2 className="font-display text-2xl font-bold">Dashboard</h2>
      <p className="mt-2 text-text-muted">
        Stats cles (commandes, CA, produits, clients) — implemente en Phase 3.
      </p>
    </div>
  )
}
